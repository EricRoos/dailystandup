version: 2.1
orbs:
  ruby: circleci/ruby@0.1.2 

jobs:
  build:
    docker:
      - image: alpine
    executor: ruby/default
    steps:
      - checkout
      - run:
          name: Build it
          command: make build
