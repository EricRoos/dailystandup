<div class="container mx-auto px-4 sm:px-8">
  <h1>Teams</h1>

  <%= render partial: 'collection', locals:  { teams: @teams } %>
  <%= link_to 'New Team', new_team_path %>
  <%= link_to 'Sign Out', destroy_user_session_path, method: :DELETE %>
</div>

